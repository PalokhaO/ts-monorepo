{
  "name": "backend",
  "version": "1.0.0",
  "description": "The backend of the sample project",
  "main": "build/index.js",
  "types": "build",
  "license": "MIT",
  "scripts": {
    "start": "tsc && concurrently npm:build:dev npm:launch:dev",
    "build": "tsc",
    "build:dev": "tsc --watch",
    "launch:dev": "nodemon --watch build build/index.js"
  },
  "dependencies": {
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.6",
    "concurrently": "^5.1.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.3",
    "typescript": "^3.8.3"
  }
}
